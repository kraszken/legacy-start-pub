services:
  etl-stable:
    container_name: etl-public
    build:
      context: .
      dockerfile: Dockerfile
      args:
        STATIC_URL_AMD64: ${STATIC_URL_AMD64}
        STATIC_URL_ARM64: ${STATIC_URL_ARM64}
    env_file: .env
    environment:
      - "HOSTNAME=^7pol^1and^7etle^1gacy^7.com ET:^1Legacy ^7Public hosted in .de"
    volumes:
      - ./maps:/maps
      - ./homepath:/legacy/homepath:rw
    ports:
      - "27960:27960/udp"
    stdin_open: true
    tty: true
    restart: unless-stopped
